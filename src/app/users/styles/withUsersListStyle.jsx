import styled from 'styled-components'

const withUsersListStyle = WrappedComponent => styled(WrappedComponent)`
  display: block;
  width : 100%;
  margin : 0;
  padding : 0;
`

export default withUsersListStyle
